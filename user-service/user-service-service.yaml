apiVersion: v1
kind: Service
metadata:
  name: user-service
spec:
  selector:
    app: user-service
  ports:
  - port: 4000
    targetPort: 4000
    name: user-port
  - port: 4001
    targetPort: 4001
    name: inter-port
  type: NodePort